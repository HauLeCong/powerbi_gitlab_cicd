# Copy to tests/.test.env and fill in values (tests/.test.env is gitignored).
# DefaultAzureCredential reads AZURE_* only (same vars as GitLab CI deploy).

AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
FABRIC_WORKSPACE_ID=
